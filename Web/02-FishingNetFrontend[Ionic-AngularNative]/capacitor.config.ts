import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.phishing.net',
  appName: 'Phishing.Net',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
